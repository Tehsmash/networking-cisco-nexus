============
Installation
============

At the command line::

    $ pip install networking-cisco-nexus

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv networking-cisco-nexus
    $ pip install networking-cisco-nexus
